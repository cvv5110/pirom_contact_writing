The main conclusion of this work are as follows,
\begin{enumerate}
    \item The PIROM framework successfully learns a ROM for the prediction of the temperature dynamics of the TPS, while simultaneously inferring the unknown contact conductances with errors of less than X\% on noisy data with SNR of up to Y dB.
    \item The PIROM outperforms the state-of-the-art data-driven, NN-based SciML, and traditional FEM modeling approaches for inferring contact conductances, as well as for predicting the temperature dynamics for OOD configurations.
    \item The PIROM is successfully deployed in an experimental setting, where it is able to infer the contact conductances and predict the temperature dynamics with errors of less than X\% on noisy data with SNR of up to Y dB.
\end{enumerate}